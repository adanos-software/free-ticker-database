name: Bootstrap canonical v4 remediation (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by bootstrap-canonical-v4-final.yml"
